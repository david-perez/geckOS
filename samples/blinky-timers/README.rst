``blinky-timers``
=================

A program that showcases how to use kernel timers (see :ref:`timers`) with
expiry callbacks in order to blink two leds. One of the leds is toggled every
second, while the other led is toggled every two seconds.

.. include:: ../building.rst
