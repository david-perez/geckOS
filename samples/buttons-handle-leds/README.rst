``buttons-handle-leds``
=======================

A program in which button :math:`x` toggles LED :math:`x`, where :math:`x =
0,1`.  It showcases how a GPIO input periperal (a button) can be used to
control a GPIO output peripheral (a LED).

.. include:: ../building.rst
